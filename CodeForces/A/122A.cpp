// 122A - Lucky Division
#include <bits/stdc++.h>
using namespace std;

int main(void) {
    int n; cin >> n;

    int temp = n;
    vector<int> digitos(4);
    while(temp > 0) {
	temp 
    }

    return 0;
}
