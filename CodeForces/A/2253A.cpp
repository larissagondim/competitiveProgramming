#include <bits/stdc++.h>
using namespace std;
#define pb push_back

int main(void) {
    int t;
    cin >> t;

    while(t--) {
	int n;
	cin >> n;
	vector<int> cards(n);

	for(int i = 2; i <= n+1; i++) cards.pb(i);    
        
	int winner = -1;
	for(int i = 0; i <= n - 1; i++) {
	    for(int j = 1; j <= n; j++) {
             
            i   

	    }
        }
    }

    }
    return 0;
}
